{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"buildCommand": "npm ci && npm run build && cd api && npm ci && npx prisma generate && npm run build"
	},
	"deploy": {
		"startCommand": "cd api && npx prisma migrate deploy && npx tsx prisma/seed.ts && node dist/server.js",
		"restartPolicyType": "ON_FAILURE",
		"restartPolicyMaxRetries": 3
	}
}
